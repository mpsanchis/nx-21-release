{
  "installation": {
    "version": "22.4.2"
  },
  "$schema": "./node_modules/nx/schemas/nx-schema.json",
  "release": {
    "version": {
      "conventionalCommits": true,
      "manifestRootsToUpdate": [
        "dist/{projectRoot}"
      ]
    },
    "git": {
      "commit": false,
      "stageChanges": false,
      "tag": false
    },
    "changelog": {
      "projectChangelogs": false
    },
    "groups": {
      "apps": {
        "projects": "apps/**",
        "releaseTagPattern": "apps@{version}"
      },
      "libs": {
        "projects": "libs/**",
        "releaseTagPattern": "libs@{version}"
      },
      "commons": {
        "projects": "commons/**",
        "releaseTagPattern": "commons@{version}"
      }
    }
  },
  "targetDefaults": {
    "build": {
      "command": "mkdir -p dist/{projectRoot} && cp {projectRoot}/package.json dist/{projectRoot}/package.json",
      "cwd": "{workspaceRoot}"
    },
    "nx-release-publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo publishing {projectName}"
      }
    }
  }
}
